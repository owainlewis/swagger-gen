package {{namespace}}

import spray.json.DefaultJsonProtocol

{{#normalized-definitions}}
{{class}}

object {{class_name}} extends DefaultJsonProtocol {
  implicit val format = jsonFormat{{arg_len}}({{name}}.apply)
}

{{/normalized-definitions}}
